# Copy to .env.local and fill in once your API Gateway is deployed.
# Vite only exposes vars prefixed with VITE_ to the browser.

# Base URL of your API Gateway stage, e.g. https://abc123.execute-api.us-east-1.amazonaws.com/prod
VITE_API_BASE_URL=
