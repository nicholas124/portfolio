# API Gateway base URL — consumed at build time by src/lib/api.js.
# Not secret (the browser calls it directly), so it's committed for reproducible builds.
VITE_API_BASE_URL=https://j0pd7gf8ob.execute-api.us-east-1.amazonaws.com
