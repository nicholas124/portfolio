node_modules
dist
dist-ssr

# Infra config (account ID, bucket, distribution + API IDs) — kept out of the public repo
.aws/
*.local
.env
.env.*
!.env.example
!.env.production
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
